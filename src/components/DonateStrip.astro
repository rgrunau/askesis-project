---

---

<div class="donate-strip">
  <p class="donate-text">
    <em>The Askesis Project is kept alive by readers who find it worthwhile.</em
    >
  </p>
  <a href="/support" class="btn-primary">Support the project</a>
</div>

<style>
  .donate-strip {
    max-width: var(--max-width);
    margin: 0 auto 2rem;
    background: var(--color-cream-04);
    border-top: 0.5px solid var(--color-cream-08);
    border-bottom: 0.5px solid var(--color-cream-08);
    padding: 2rem 2.5rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
  }

  .donate-text {
    font-family: var(--font-serif);
    font-size: 1.05rem;
    line-height: 1.6;
    color: var(--color-cream-55);
  }

  @media (max-width: 600px) {
    .donate-strip {
      flex-direction: column;
      align-items: center;
      text-align: center;
    }
  }
</style>
